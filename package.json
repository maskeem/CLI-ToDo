{
  "name": "cli-todo",
  "version": "1.0.0",
  "description": "Test technique pour recrutement sur un poste back-end. Création d'une to-do list en CLI avec connexions multiples simultanées. Serveur Node.js, BDD MongoDB.",
  "homepage": "https://github.com/maskeem/CLI-ToDo#readme",
  "bugs": {
    "url": "https://github.com/maskeem/CLI-ToDo/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/maskeem/CLI-ToDo.git"
  },
  "license": "ISC",
  "author": "",
  "type": "module",
  "main": "server/server.js",
  "scripts": {
    "server:dev": "node --watch --env-file=.env server/server.js",
    "server:start": "node --env-file=.env server/server.js",
    "client:dev": "node --watch --env-file=.env client/client.js",
    "client:start": "node --env-file=.env client/client.js",
    "client:test": "node --env-file=.env test/test-client.js",
    "db:seed": "node --env-file=.env database/scripts/seed.js",
    "db:test": "node --env-file=.env test/test-db.js"
  },
  "dependencies": {
    "mongodb": "^6.17.0"
  }
}
